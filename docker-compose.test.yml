version: '3.9'

services:
  cypress:
    image: cypress/included:9.1.0
    entrypoint: []
    command: /bin/bash -c 'npm install dayjs wait-on --save && npx wait-on http://localhost:8000 && cypress run'
    working_dir: /e2e
    environment:
      - CYPRESS_baseUrl=http://localhost:8000/
    depends_on:
      - nlp
      - db
      - elastic
    volumes:
      - ./e2e:/e2e

  nlp:
    environment:
      - SERVER_NAME=localhost:8000

volumes:
  data01:
    driver: local
